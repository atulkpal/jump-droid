# Tasks - Six-Layer Parallax Background System

- [x] Research existing background and parallax system.
- [x] Update `ParallaxSystem.kt` to include `BitmapParallaxLayer` with horizontal tiling.
- [x] Fix vertical scrolling direction (Background moves DOWN as player moves UP).
- [x] Add `clearLayers` to `ParallaxManager` for switching modes.
- [x] Update `ZoneBackgroundRenderer.kt` to initialize Layers 2-6 from assets.
- [x] Preserve procedural Layer 1 (Gradients).
- [x] Add memory management (clear asset cache on zone transitions).
- [x] Update `DevConfig.kt` to enable `RENDER_MODE_ASSETS`.
- [x] Verify procedural base + asset overlay logic.
- [x] Create walkthrough artifact.
